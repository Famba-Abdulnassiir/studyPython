# Just like another language python also has the same assingment operators
#e.g += -= /= **= *= etc we maynot have an example here but thats much enough

numbers = 3
numbers += 3  # this is like numbers = numbers + 3, its shorter and clean. u can also use other operators here *=
print(numbers)